# Report templates (v3.4)

Markdown and Python modules for `POST /reports/generate` land here per Part B of `POLARIS_FINAL_EXECUTION_BLUEPRINT.md` (e.g. `quarterly_performance.md`, `intervention_priority.md`, `executive_summary.md`, `recommendations.py`).

This directory is scaffolded for Sprint 1; template files are added in a later sprint.
